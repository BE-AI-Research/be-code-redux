# SDD ledger — plan: docs/superpowers/plans/2026-09-17-context-handling.md
Spec: docs/superpowers/specs/2026-09-17-context-handling-design.md (read; binding authority)
Branch: feature/context-handling (be-code), base for the whole branch: 2bd4f5c..see git merge-base main HEAD

## Preflight scan

Pairs that share a file or an interface:

| tasks | produced → consumed | finding |
|---|---|---|
| 1 → 2 | `Evidence` placeholder in node.go → filled by record.go | clean |
| 2 → 3 | `recorder`, `Limits`, `Event` → embedded by `Store`, `Open(…, lim Limits)` | clean |
| 3 → 4 | `Store` tree + evidence API → `Report`, `Render` | clean; `testStore` helper defined once in report_test.go |
| 3 → 5 | `Plan/Add/SetStatusText/Note/ShowText/EnsureRoot` → `tools.TaskLedger` | clean after the plan's SetStatusText patch |
| 5 → 6 | `Store.TreeText/ShowText` → `ui.TaskLines` | clean |
| 7 → 8 | `provider.Options`, `SetOptions`, `Status` → `loader.Apply` | clean |
| 7 → 9 | `provider.Details` → `/models` rows | clean |
| 8 → 9 | `loader.Loader` methods → `agent.ModelLoader` interface | clean; names match exactly |
| 8 → 9 | `applyBackendWindow` deleted (8) vs `checkBackend` edited (9) | clean; different functions |
| 3 → 10 | task documents → e2e sentinel assertions | clean |

Per task, does its own text agree with itself: T1 ✓, T2 ✗ (see ruling P-1), T3 ✓, T4 ✓, T5 ✓ (test helpers undefined, ruling P-2), T6 ✓ (same), T7 ✓, T8 ✓, T9 ✓ (same), T10 ✓ (prose steps, no code contract).

Ruling P-1: Task 2's interface block re-declares `Range`, `Hit`, `Event`, `relTo`, `parseHits`, `mergeRange`, `covered`, `seenRange` and `LookupKey`, which all already exist in the package (store.go, observe.go). Declaring them again breaks the build for Tasks 1–2, which would stop even a `-run`-scoped test. Task 2 therefore declares ONLY what is new (`FileRef`, `CmdRef`, `LookupRef`, `NoteRef`, `RawItem`, `Evidence`, `Limits`, `recorder`, `excerpt`, `argsLine`, `firstLine`) and reuses the existing helpers in place; Task 3 moves them into record.go when it deletes observe.go. Costs a small reshuffle in Task 3 if wrong.

Ruling P-2: the plan's test snippets use helpers it never defines (`testAgent`, `agentWithEngine`, `agentWithBadEngine`, `containsAny`, `waitFor`, `fillHistory`, `emptySummaryProvider`, `evictedProvider`, `windowChangedProvider`, `testStoreFor`, `newTestSession`, `newTestView`). Each implementer defines the ones its task needs in its own test file, reusing an existing equivalent where the package already has one. Costs a little duplication across test files, which is cheaper than a shared test helper package.
Task 1: implementer DONE at 5a253cb (5 new tests, 28 package tests, verify green); reviewer dispatched (sonnet)
Task 1: review (sonnet) — Spec ✅ / Quality approved; diff byte-identical to the brief. Minors (deferred): (1) Add accepts empty text (plan-prescribed); (2) re-opening a terminal node clears Closed but has no test; (3) Find/Doing/ActiveBranch hand out live *Node pointers, so a caller can bypass SetStatus's invariants — worth a comment when Tasks 2-3 consume them.
Task 1: complete (commits 719afd9..5a253cb, review clean)
Task 2: dispatched (BASE 5a253cb, implementer sonnet)
Task 2: implementer DONE at 9826e5a (33 package tests, verify green). Reported seams for Task 3: seenRange's real signature is seenRange(ev Event, total int); relTo/fileState are *Store methods so the recorder got its own rel/readWorkspaceFile over the existing free helpers; readFooter scans the node's own Raw buffer because FileRef has no Turn and Files is only populated at distill; markEdited is a no-op stub until Files exists; recorder.rel's absolute-path branch is untested until the recorder has a real root.
Task 2: reviewer dispatched (sonnet)
Task 2: review (sonnet) — Spec ✅ / Quality changes requested. Critical: mergeFile never checks it.OK, so a FAILED read/write/edit is merged as success — Edited=true on a failed write, and Ranges polluted by seenRange over the error text. Important: firstLine("") drops a failing call with empty output entirely, which for file tools is total loss of the failure. Minors: a single raw item larger than NodeCap is never dropped (len>1 guard); RawItem.Args is counted but never excerpted; excerpt's newline branch requires i>0. All five implementer deviations accepted.
Ruling T2-a: spec §4.2 wants an error to carry the command that produced it, and the brief's Errors []string cannot. Store it as "<args>: <first line>" rather than changing the type — keeps the shape every later task consumes; costs a parse if anything ever needs the two halves apart.
Ruling T2-b (carry into Task 3): the redundant-read footer is now per-node and limited to the live buffer, where 0.10.0's was store-wide across sessions. Task 3 must decide that deliberately when it embeds the recorder in the store, not inherit it by accident.
Task 2: fix round 1/5 (Critical + Important + two minors; resume implementer)
Task 2: fix round 1/5 (4 addressed, 0 open; commit a2ab257) — re-review: all ADDRESSED, both new tests verified non-vacuous by hand against the pre-fix logic. New latent minor: readFooter and mergeFile resolve paths from the now-excerpted RawItem.Args, so a path rendering longer than ItemCap would silently fail to match (unreachable at realistic caps).
Task 2: complete (commits 5a253cb..a2ab257, review clean after 1 fix round)
Task 3: dispatched (BASE a2ab257, implementer opus)
Task 3: implementer DONE_WITH_CONCERNS at e2537ca (29 engine tests, verify green, -race clean). Ruling T2-b decided store-wide/tree-wide/hash-aware, surviving sessions, with a raw-buffer fallback; FileRef gained Turn and file evidence merges continuously, which also fixed a latent Task 2 bug (a file edited between two reads in one node would have produced a footer for content the model never saw). Helpers unified on one foldPath and one fileState/snapFile; RawItem gained Path resolved from the original Event, closing Task 2's latent minor. Added two files the brief did not name: migrate.go and compat.go (a temporary 0.10.0 flat projection so Tasks 4-5 can rewire deliberately; its header says they delete it).
Ruling T3-a: /task clear must NOT delete task documents. Deleting a user's file in their own project on one keystroke is not a reset, it is data loss, and the guard (only delete when the hash still matches what we wrote) protects the careful user and destroys the record of the one who never opened the file. Clear instead closes any open roots as dropped with reason "cleared" and resets the session's active node and buffers; the documents stay as history. Costs a longer /task listing after a clear, which /task show and the ladder already handle.
Task 3: reviewer dispatched (opus)
Task 3: review (opus, probes in scratchpad) — Spec ❌ / Quality changes requested. CRITICAL, all three are data loss: (1) /task clear + Flush deletes the user's task documents (contradicts T3-a); (2) migration deletes ledger.json/digests.json/lookups.json before the lifted tree is ever flushed, so Ctrl-C at the first prompt loses all working memory permanently; (3) a hand-added second top-level item makes Flush rename over the file it just wrote, destroying the first task's document. Important: (4) any top-level "- [ ] x" a user writes becomes a task, while the README promises a checklist is preserved; (5) .gitignore and README.md are written non-atomically into the user's repo; (6) edit_file claims a whole-file range, so a never-read file can produce an "already read" footer; (7) the riskiest new behaviour (doc deletion, clear, interrupted migration, evidence carry-over, .gitignore edge cases) has no test. Minors 8-12 recorded. Deep-copy discipline confirmed intact.
Ruling T3-b: state.json gains a files map (path → hash, outline, turn). The reviewer is right that without it the cross-session footer T2-b promises cannot work and outlines are lost on every resume — a ruling that delivers nothing is worse than one not taken. Costs a schema field and its migration.
Ruling T3-c: a top-level checklist line a human writes is NOT a task. Only a depth-0 line carrying an id, or the first root of a document that has none, becomes one; anything else is preserved as their own text, which is what the README already promises.
Task 3: fix round 1/5 (3 Critical, 4 Important, minors 9 and 10; resume implementer)
Task 3: fix round 1/5 implementer DONE at 1eaca21 (38 engine tests, 9 new, each probed failing against the pre-fix code; -race and verify green). Went further than asked on Critical 3: removed document renaming entirely rather than reordering it, on the reasoning that renaming is the only thing that ever made the engine want to remove a file, and removal is what T3-a forbids; cost is a file name that can fall out of step with a retitled task. Also fixed an unreported bug: ParseDoc numbered every root '1' per document, so every root after the first had its whole subtree 'repaired' on every load, accreting two bogus notes each time.
Task 3: fix round 1/5 re-review (opus, own probes) — all 9 findings ADDRESSED; both beyond-instruction changes judged better/correct (no orphan or duplicate document through retitle x3; ids stable over five load+flush cycles with zero repair notes). New: D1 Important, an interrupt between Flush and the ledger remove re-migrates on the next open (two roots, two documents); D2 Important, a document the user emptied of bullets frees its name, so a later task with that slug overwrites their prose; D4 Important, restoreFileMemos gives the newest hash to every node's ref for a path, reviving stale ranges so the edit_file fix stops holding across a reopen; D3 Minor, an evidence key under a declined checklist item is adopted by the previous task; D5 Minor, the NNN prefix can repeat.
Task 3: fix round 2/5 (D1, D2, D4, plus the two minors; resume implementer)
Task 3: fix round 2/5 implementer DONE at aa159bb (43 engine tests, 5 new, each probed failing pre-fix; D4 reproduced the reviewer's exact 'already read at turn 2'). D1 sets migrated on the Store before the flush that writes it and finishes the interrupted tidy-up rather than abandoning a stale ledger.json; D2 reserves rootless documents' names; D4 pins the hash to the owning node id while outline and turn stay shared as facts about the file.
Task 3: fix round 2/5 re-review (opus, own probes) — D1 addressed for its window but reopened in an adjacent one, D2/D3/D4/D5 addressed. New: (1) Important, `migrated` reaches disk only in state.json, which Flush writes AFTER the documents, so a kill in that window yields 001-lift-me.md + 002-lift-me.md; (2) Important and silent data loss, the flag is permanent so any later ledger.json (a downgrade, a restored backup) is removed unlifted — the probe lost a task outright; (3) Minor, fileMemo pins by node id, which is positional, so a hand-deleted step lets a stale node inherit the owner's hash.
Ruling T3-d: the migration never deletes anything. It renames the legacy files aside to <name>.migrated-<stamp> and keeps them. That removes defect 2 entirely (a restored backup is a different file and migrates normally), and it makes idempotence a question of what is on disk rather than a permanent flag. Order: rename aside first, then build and flush; if the flush fails, rename back so the next open retries. Costs a few kilobytes of dead files in the dotdir, which is the cheapest insurance on this branch.
Task 3: fix round 3/5 (T3-d, plus defect 3; resume implementer)
Task 3: fix round 3/5 implementer DONE at 00398a0 (45 engine tests, 3 new probed failing pre-fix, 1 removed). T3-d implemented: setLegacyAside renames, order is rename → build → flush, a failed flush undoes the tree and renames the files back; state.Migrated removed outright. Its own new test caught data loss inside the fix — two migrations in the same second produced the same stamped name and os.Rename replaced the destination, destroying the first preserved copy; freeName now walks -stamp, -2, -3. Removed round 2's interrupted-tidy-up test because under T3-d a restored ledger is indistinguishable from a backup and must lift, so the test asserted what the ruling forbids.
Ruling T3-e: accept the residual window rename-first creates — a kill between the rename and the flush leaves the old store as ledger.json.migrated-<stamp> with nothing lifted, recoverable by renaming it back. It is microseconds wide with no model call in it, nothing is destroyed, and the alternative it replaced failed by silently duplicating every task. Candidate for the final review: lift automatically from an aside file when the store is otherwise empty.
Task 3: fix round 3 scoped re-review dispatched (opus)
Task 3: fix round 3/5 re-review (opus, own probes) — items 1, 2, 3, 5, 6 all close on the reviewer's own evidence (restored ledger lifts and keeps both; six same-second migrations give six distinct aside files; a directory in the way falls to -2; read-only dotdir refuses cleanly; fileMemo by id+ranges has no false refusals; the removed test's deletion is correct). One blocker: Important, migrate.go:130 — when Flush fails AFTER the documents are written (a full disk, an unwritable dotdir), undo renames the legacy files back but leaves 001-lift-me.md on disk, so the next open loads that document AND re-migrates the restored ledger: two roots, two documents. The duplicate-task defect reinstated through its own undo. Minors: migrate.go:163 treats any non-IsNotExist stat error as occupied and loops; a state.json.tmp orphan is left behind.
Ruling T3-f: on a failed flush the migration does NOT restore the legacy files. It keeps the aside and keeps the documents already written, because at that point the lift has happened — only the dotdir state failed to persist, and the next open reads the documents and finds no ledger, so it neither duplicates nor re-migrates. This keeps "never delete anything" absolute: the alternative fix deletes documents, and a rule with an exception is a rule that gets exercised. Costs a lost turn counter and baseline on that one failure.
Task 3: fix round 4/5 (undo path + two minors; FRESH implementer on opus, per the rounds-4-5 escalation)
Task 3: fix round 4/5 implementer (fresh, opus) DONE at c6ff626 (47 engine tests, 2 new; -race and verify clean). Flush returns a flushError carrying wroteDocs; migrateLedger splits the cases — documents written means nothing restored and nothing removed, returning unsavedStateError, with OpenAI warning that the lift happened but its state could not be saved; nothing written keeps the old undo/retryable path. undo left untouched and now runs only pre-document: truncating would leave memory disagreeing with a document on disk, and since the store stays dirty the next flush would rewrite that document from the truncated tree and destroy a lift that had succeeded. Minors: freeName returns an error and bails on a real stat error rather than spinning; writeAtomic removes its temp on both failure paths. Noticed, untouched: setLegacyAside swallows real stat errors the same way freeName did.
Task 3: fix round 4/5 re-review (opus, own probes) — original defect gone, pre-document path unchanged, wroteDocs accurate in the tested cases, a dirty store does persist turn and baseline on the next flush, error shapes clean, both minors fixed. New Important, a regression of this round: store.go:649 — wroteDocs means "some document", not "the migrated root's document", so with documents already present and the failure landing on the migrated root's own file, the aside is kept, the ledger is not restored, the lift never reaches disk, and the warning falsely claims the documents were written. Round 3's undo handled that case correctly. Minor: setLegacyAside's swallowed stat error can leave a half-moved set.
Task 3: fix round 5/5 (wroteN, the reviewer's named smallest change, plus the setLegacyAside stat; resume the round-4 implementer)
Task 3: fix round 5/5 implementer DONE at a44ec6c (49 engine tests, 2 new, both probed failing pre-fix with the reviewer's own scenario and the false warning reproduced; -race and verify clean). wroteDocs became wroteN; the lift counts as on disk only when wroteN > roots taken before the lift appended its root, so the migrated root's own document is the roots+1-th write; below that it falls through to undo exactly as a pre-document failure does. setLegacyAside now restores what it moved and fails retryably on a real stat error.
Task 3: fix round 5/5 re-review (opus, own probes) — both items fixed, counting invariant verified at every boundary (zero pre-existing roots, all documents failing, a middle document failing, all written with state.json failing), nothing earlier regressed. New Important but PRE-EXISTING, not from this diff: store.go:650 — a document the user added a second task to is split across two files, and Flush rewrites the source document before writing the split-off root's new file, so a failure on that second write erases the hand-added task (probe: tree back to [beta], gamma gone). Minor pre-existing: restoreLegacy discards rename errors, so a failed restore silently leaves the store as *.migrated-* and never retries.
Ruling T3-g: park the split-root loss, and mark it MUST-FIX for the final whole-branch fix wave rather than triage. It is the same class the whole task has been about, losing the user's own hand-added task, but it needs a mid-flush I/O failure on a hand-edited multi-root document, the reviewer confirms it is not load-bearing for Task 4 (renders) or Task 5 (wiring), and the fix is known and three lines: in Flush, write roots whose name is new before rewriting the document they were split out of. Costs that narrow window staying open until the final wave.
Task 3: complete (commits a2ab257..a44ec6c, 5 fix rounds, 2 parked: the split-root loss (T3-g, must-fix at final) and unsavedStateError's lost turn counter (T3-f, accepted))
Task 4: dispatched (BASE a44ec6c, implementer sonnet)
Task 4: implementer DONE_WITH_CONCERNS at 3522174 (49 engine tests green; vet and build clean) BUT the branch is RED: 4 tests in internal/agent/engine_test.go fail, asserting the old flat-ledger phrasing and one 0.10.0 assumption the redesign intentionally changes. The implementer declined to touch that file because the plan assigns it to Task 5, and says one of the four raises a real design question: whether a task should be visible before anything is marked doing.
Ruling T4-a: a task does not end with the branch red. The three format-only assertions are mechanical and belong to whoever changed the format, which is this task; the fourth is a render question, not a wiring one, so it is also this task's. Task 5 still owns the wiring itself. Costs Task 4 one extra fix round.
Ruling T4-b: the block shows the newest open root's branch when NO node is doing, rather than nothing. A task the model has planned but not started is exactly when it most needs to see the plan, and "invisible until you mark a step doing" would train the model to skip planning. Costs a few lines in the block when a root is open and idle.
Task 4: reviewer dispatched (sonnet)
Task 4: review (sonnet) — Spec ❌ / Quality changes requested. The four failing agent tests: two cosmetic (old "Task:"/"doing: N." phrasing; the block is correct under probe), one a real gap (a root opened by StartTask with no doing child is invisible: Render's roots slice only takes Terminal roots and activeBranchText returns "" when Doing() is nil — T4-b confirmed NOT satisfied), one an outdated assumption (compaction now deliberately folds the doing node's raw buffer into the summarization request). Important: Node.Evidence.Dropped is never read by report.go or render.go, so a node whose buffer was capped renders as if complete — the record does not admit the gap it was built to admit. Minors: trimLines is now dead in production; the ladder recomputes every report's text per rung.
Task 4: fix round 1/5 (T4-b fallback, surface Dropped, update the four agent tests, two minors; resume implementer)
Task 4: fix round 1/5 implementer DONE at 6c76326 (full repo green: engine, agent, -race, vet, build, verify). T4-b fallback implemented and tested; Dropped now surfaces in both the report and the raw block; all four agent tests owned and fixed rather than deferred; trimLines wired back in with a budget<=0 edge it caught; the ladder re-renders only the changed report.
Task 4: fix round 1/5 re-review (sonnet, own probes) — all four ADDRESSED, no new defects. T4-b's fallback shares store.go's own "last non-terminal root" definition and is disjoint from the reports section by construction; Dropped is a real counter that survives distillation and appears at rung 0 and in the raw block; explicitly confirmed NONE of the four agent tests was weakened — TestRunFullRefreshesTheTaskLineBetweenRequests passes because StartTask retitles the still-open root and T4-b makes it visible, and TestCompactUsesDigestsAndFeedsFileNotesBack was narrowed to the transcript section, which still fails if the transcript leaks raw content.
Task 4: complete (commits a44ec6c..6c76326, review clean after 1 fix round)
Task 5: dispatched (BASE 6c76326, implementer opus)
Task 5: implementer DONE_WITH_CONCERNS at d01a62f (-race and verify green; 12 new tests, 1 renamed to the new compaction contract, ~10 mechanically updated for the removed compat API). Concerns: (1) observe's new 15 s timeout+goroutine bounds only the recorder hook, while Render/EnsureRoot/StartTask/Flush/Cached/HasDigest/ApplyFileNotes stay synchronous, so a store wedged elsewhere still parks the turn; (2) a timed-out observe abandons a goroutine holding the store lock, so every later observation that session times out too, with a notice each and no circuit breaker; (3) LedgerText's 0.10.0 flat projection survives privately in render.go because /task in both UIs still calls it until Task 6; (4) taskGuidance left verbatim; (5) new config keys engine.item_cap and engine.node_cap, documented; (6) Tree.Add now allocates one past the highest index a parent ever handed out, because adoption removes the unfiled node from the middle of the children.
Ruling T5-a: taskGuidance stays verbatim this task. The paragraph still reads correctly against the new verbs, and a memory note records that a short prompt change here moved the local model's behaviour a lot — that is a change to make deliberately on the VM with the user watching, not as a side effect of a wiring task. Parked for the final review.
Task 5: reviewer dispatched (opus)
Task 5: review (opus, probes) — Spec ✅ / Quality changes requested. CRITICAL: (1) store.go:1192 adoption can remove a ROOT-level unfiled node while s.files/s.extra stay index-parallel to Roots, so every later root shifts one document down and a reload yields a DUPLICATED task; (2) node.go:47,54 roots still allocate len(Roots)+1 while only children got the highest-index-ever rule, so after an unfiled root is removed the next Plan returns an id that collides with a live task and Find resolves to the wrong node — a status done then closes the wrong task. Important: (3) the new observe timeout does not save a session — a store wedged on its own mutex makes the very next composeSystem → Render block forever (probe: deadlock), while each tool call leaks a parked goroutine, stalls 15 s and emits a notice; (4) a CANCELLED summary (Esc, /compact) now takes the task-record path and destructively rewrites history instead of erroring. Minors: the "hang" test wedges the seam, not the store lock, so it proves the timer fires and nothing more; a legacy {"action":"step","step":2} routes to root 2 rather than step 2; the adoption id test covers only the child case.
Ruling T5-b: revert observe to the plain panic fence. A timeout that cannot save the session is worse than none: Render blocks on the same mutex a moment later, so the bounded failure becomes an unbounded one plus a leaked goroutine and a notice per tool call. Bounding the store properly needs an Engine interface behind which every call is timed, which is later work, not a wiring task. Costs losing a bound that never held.
Task 5: fix round 1/5 (2 Critical, 2 Important, 3 minors; resume implementer)
Task 5: fix round 1/5 implementer DONE at 8791a04 (4 packages ok, -race clean on rerun, verify green; 3 new tests + 1 extended, each probed against the pre-fix code). Worth recording: two of its own probes PASSED pre-fix at first — the root-duplication needs a flush while the unfiled root exists AND another after the next root is created, and its first id assertion was wrong because Tree.Find returns the last match — both corrected rather than shipped as false confidence. T5-b applied: goroutine, timer and const gone, plain defer/recover restored with the ruling in the doc comment; the observeFn seam and the 4 KiB footer cap stay. fromTaskRecord now takes ctx and refuses when done, silently. The step-shaped legacy call resolves a bare integer under the active root via an optional ActiveRootID() capability that noopLedger does not implement.
FOLLOW-UP for the final review (from T5-b): no engine call is time-bounded; a wedged store still takes the session at the next composeSystem. The fix is one interface with every call timed behind it.
Task 5: fix round 1 scoped re-review dispatched (opus)
Task 5: fix round 1/5 re-review (opus, own probes) — all five findings genuinely fixed and genuinely tested. Honest correction to the implementer's claim: the engine, tools and agent tests DO fail pre-fix, but the cmd/engine_test.go line passes pre-fix and is a regression guard, not fix-proving. New Important: store.go:628 and render.go:38 treat the emptied-and-dropped root-level unfiled node as a first-class root, so Flush writes 002-unfiled.md into the user's .be-code/tasks/ (which the engine never deletes, by T3-a) and Render emits it as its own dropped report. Minors: an empty summary while cancelled still prints the diagnostic to stderr; cerr at loop.go:1006 is bound and unused.
Ruling T5-c: fix the unfiled leak now rather than parking it. The reviewer calls it a two-line follow-up and offers to let the task close, but it puts a file the user never created into their repository and a phantom task into the model's prompt, and nothing deletes it afterwards. Costs one small round.
Task 5: fix round 2/5 (unfiled leak + two minors; resume implementer)
Task 5: fix round 2/5 implementer DONE at ef5b25d (4 packages ok, -race clean, verify green). spentUnfiled(n) skipped in Flush's write loop and Render's terminal-roots loop while docNamesLocked still claims the name so later roots keep their numbering; both new tests fail pre-fix. Corrected an assertion rather than loosening it: on this path Plan closes the in-flight unfiled node on its way past, so the adopted evidence arrives distilled, not raw. Residual, by T3-a: a spent unfiled root whose document an EARLIER flush already wrote keeps that file; only the flush following adoption in the same request — the path the recorder actually takes — is silent.
Task 5: fix round 2/5 re-review (sonnet, own probes) — all three fixes correct, both new tests genuine (reverted each guard in place and reapplied). Confirmed the distillation claim in code: Plan calls closeDoingLocked before creating the new root, so the adopted evidence really does arrive distilled. Confirmed the T3-a residual is unreachable from the recorder, because run() always calls EnsureRoot before any Observe. New Minor: spentUnfiled matches on text and childlessness alone, so a real task titled exactly "unfiled" with no subtasks would silently get no document and no report, and a root-level unfiled node still doing would be suppressed in Flush.
Ruling T5-d: park the spentUnfiled predicate gap for the final fix wave with the one-line fix already written down — also require StatusDropped and a reason starting "adopted by ". It is unreachable through the recorder today, but it is a silent no-document case keyed on a word a user could plausibly type. Costs nothing to carry; the final wave applies it.
Task 5: complete (commits 6c76326..ef5b25d, review clean after 2 fix rounds, 1 parked)
Task 6: dispatched (BASE ef5b25d, implementer sonnet)
Task 6: implementer DONE at 3e2e55f (ui, tui, engine, cmd green; -race and verify green). Documentation-accuracy note it raised: the 'exactly one node is doing' rule is enforced only along the path the engine drives (SetStatus demotes the previous doing node); a hand-edited document with two [>] marks is neither detected nor repaired, and the engine silently keeps whichever it reads last — the README says so honestly rather than implying auto-repair. It also caught a factual error in its own earlier README draft, conflating the project-local .be-code/ with the home-dir ~/.be-code/. Reviewer dispatched (sonnet)
Task 6: review (sonnet, own probes) — Spec ✅ / Quality approved. README verified claim by claim against the code: the worked example round-trips byte-identical, all five marks match markdown.go, the id-repair note is character-for-character what the code emits, quarantine naming and its stderr line match loadDocs, and the .gitignore line matches ensureGitignore. Important: a hand-edited document with two [>] marks is silently resolved to whichever is read last, with no warning, while id-repair gets an in-document note and quarantine gets a stderr line — the two other "silent to the model, visible to the human" paths. Minors: the README claims a non-repo folder cannot be hidden, but ensureGitignore still appends when a .gitignore exists; the README says the engine never renames a document, contradicted by its own quarantine section; /task open prints the directory before any document exists.
Ruling T6-a: add the stderr warning for multiple doing marks, and do not touch the document. Spec 5.2 says a hand-edited status is the user's intent, so overriding one silently is wrong, but so is saying nothing while the engine quietly picks one — the other two failure paths both tell the human. Costs one line on load.
Task 6: fix round 1/5 (multi-doing warn + three minors; resume implementer)
Task 6: fix round 1/5 implementer DONE at 2b927ac (ui, tui, engine, cmd green; -race and verify green). warnMultipleDoing called from loadDocs prints one warn: line naming every document carrying a doing mark, touching neither document (spec 5.2 honoured literally); both README inaccuracies corrected; /task open gated on a new HasTaskDocuments() so it says 'no task documents yet; the first one will be written to <dir>' when empty. TestWarnsOnMultipleDoingMarks verified failing pre-fix by reverting the call.
Task 6: fix round 1/5 re-review (sonnet, own probes) — all four fixes PASS, no new defects. The warning fires once per open, dedupes by document, never fires for one or zero doing nodes, goes straight to stderr from loadDocs and is unreachable from prompt composition, and neither document is rewritten. HasTaskDocuments correctly reads false for a README-only or quarantine-only directory. Correction to the implementer's report: TestPlainTaskOpenIsTruthfulBeforeAnythingIsWritten is not merely a guard — reverting just the repl.go gating reproduces a real failure.
Task 6: complete (commits ef5b25d..2b927ac, review clean after 1 fix round)
Task 7: dispatched (BASE 2b927ac, implementer opus)
Task 7: implementer DONE_WITH_CONCERNS at 46dd2e8 (provider 21/21 -race, verify green). Decisions: native-unsupported is 404/405/501 only, matched on a typed *httpError with errors.As and latched in an atomic.Bool — a 400 or 500 is returned as-is, because a 400 is the server rejecting THIS request and downgrading on it would hide a fixable error and silently cost num_ctx for the session; the error string stays byte-identical so agent.looksLikeToolsUnsupported still parses it. NoThink loses its branch but the body is NOT the brief's literal "think": !req.NoThink — true is a 400 on a non-thinking model, so the key is omitted unless asked for (false for NoThink, else the ReasoningEffort level, which the native path would otherwise silently drop), with a 400 naming think dropping the key for that model and retrying once. Unrequested fix: NewOllama now derives both bases so a provider configured as a bare http://host:11434 (the spec's own example) no longer posts OpenAI requests to a /v1-less path. Concerns: SetOptions/SetKeepAlive have no caller until Task 8, so the num_ctx win lands there together with the reload-consent gate; think levels are unverified against a real Ollama; Details is unused so far.
VM-VERIFICATION ITEM for the user: the think levels on the native path need one run against the LAN box with a thinking and a non-thinking model.
Task 7: reviewer dispatched (opus)
Task 7: review (opus, 12 probes) — Spec ❌ / Quality changes requested. All three named silent-failure risks pass (NDJSON split lines and a 3 MiB line, args object→string with {} for absent, ids synthesized for two calls in one chunk and across chunks). CRITICAL: ollama.go:211-221 treats any 404 as "old server", but a modern Ollama returns 404 from /api/chat for a model that is not pulled — probe shows one typo in a model name latches native off for the session, taking num_ctx with it, and it never comes back after /model. Important: the fallback is silent because nothing exports nativeBroken, so the spec's "with a notice" can never fire; one latch conflates "cannot think" with "levels unsupported", so a server accepting only boolean think makes every later NoThink call (compaction, handoff, init) silently reason again; Temperature 0 is now omitted where the OpenAI path always sent it. Minors: the think-retry fires even when no key was sent; the test justifying decision 1 uses the wrong wire shape (400 rather than 404 + JSON body); the fallback test asserts only absence of error; a non-/v1 OpenAI base gets /v1 appended twice.
Decision verdicts: the 400/500/transport exclusion is right in principle but drawn at the wrong line; the think deviation is endorsed and kept (the brief was wrong — "think": true 400s on a non-thinking model, and carrying ReasoningEffort as think is the same translation Ollama's own OpenAI layer does); the NewOllama base fix is correct and necessary.
Task 7: fix round 1/5 (Critical + 3 Important + 4 minors; resume implementer)
Task 7: fix round 1/5 implementer DONE at 23e50bf (provider 25/25 -race, agent ok, verify green; every new test stated as failing pre-fix). isNativeUnsupported now needs a route-shaped body as well as the status; NativeFallback() exported behind a new provider.NativeFallbacker and noticed once per session from chatWithRetry; noThink and noThinkLevels latched separately with thinkRefusal defaulting to the narrower fact when ambiguous, pinned by a wire-sequence test; temperature always on the wire; the four minors done.
Task 7: fix round 1/5 re-review (opus, 13-case matrix + end-to-end probes) — all four findings fixed and pinned; the four new tests genuinely fail pre-fix. A not-pulled 404 now surfaces as an ordinary error the agent reports rather than downgrading or spinning retries; the notice fires exactly once on the flip, from the agent goroutine, reaching both UIs without a transcript race; the two latches converge with a model that refuses both, and the ambiguous default cannot strand a thinking model.
Task 7: complete (commits 2b927ac..23e50bf, review clean after 1 fix round, 3 minors carried to Task 8)
CARRY INTO TASK 8 (all three from Task 7's re-review, and Task 8 is where SetOptions finally has a caller): (a) ollama.go:123 — a request naming temperature 0 with a configured non-zero sends the configured value, contradicting the fix's own rationale; drop the fill-in or make Options.Temperature a *float64, and cover temperature with a test, which nothing does today; (b) resilience.go:101 — nativeFallbackNotified is never reset when Provider is replaced (repl.go:500, view.go:1599), so a real downgrade on a newly selected provider is silent; (c) ollama.go:240 — an nginx or Apache HTML 404 with no "page" in front of a genuinely old Ollama never downgrades and the session fails hard forever; also accept a 404 whose body does not decode as JSON carrying an error field.
Task 8: dispatched (BASE 23e50bf, implementer opus)
Task 8: implementer DONE_WITH_CONCERNS at 2f832e0 (-race all green, verify clean, 31 new tests incl. the brief's 8 verbatim; real config untouched). Concerns: (1) the startup consent ask is DEFERRED — buildAgent runs before any UI wires Registry.Approve, so a startup mismatch is refused safely with a stderr notice naming reload_on_mismatch: "always", not the [r]/[k]/[a] prompt the spec describes; the mechanism is complete and a nobody-to-ask refusal deliberately does not latch, so the question stays askable, but the callers are Task 9's; (2) loader notices go to stderr, which for a served session is the host log; (3) config.Default() still sets context_tokens 16384, so a user setting context_window 32768 gets a 32768 window and a 16384 budget until they clear it; (4) Apply uses a new Ollama.Resident (reads /api/ps only) rather than Status, because Status falls back to /api/show and would have failed the brief's own no-probe test; (5) carried fix 1 resolved by deleting Options.Temperature outright, since options: {"temperature": ...} covers it.
Task 8: reviewer dispatched (opus)
Task 8: review (opus, 10 probes via -overlay) — Spec ❌ / Quality changes requested. CRITICAL: loader.go:137-143 — when /api/ps ERRORS, the loader assumes the configured window and puts it on the wire unasked (probe: nginx 502 and a probe timeout both gave num_ctx=32768, asks=0, no notice), so a model another application holds at 8192 gets evicted with no consent; the sibling unreported-window branch at 152-161 already does this correctly. Important: asked/agreed are read and written with the lock dropped across approve, so 8 concurrent Applies produced 8 prompts and a first-yes-rest-no run returned 32768 to one caller while the wire ended at 8192. Important: context_tokens 16384 in Default() silently caps a configured 32768 window, and ApplyWindow returns clamped=false so NOTHING is printed — the user's original complaint, invisible because the new cmd tests all zero the field first. Minors: sessionLoader's Approver is written unsynchronised; both temperature tests pass against the pre-fix code, so the deletion is the guarantee, not the tests, and SetProvider's latch-clearing has no test.
Verified good: the no-probe guarantee (/api/show 0, /api/generate 0, /api/ps 1), applyBackendWindow and its 4-minute warm gone, all three reload_on_mismatch values plus an unknown one, a refusal putting the SERVER's window on the wire, every new key in the README.
Ruling T8-a: Task 9's acceptance now explicitly includes (a) re-running Apply on a goroutine once Registry.Approve is wired in runInteractive/runSessionHost, and (b) loader notices through Agent.Events rather than stderr. Without both, spec 9.3's prompt exists nowhere in the shipped product and a default-config user silently runs at whatever the server held. The reviewer accepts Task 8's boundary only on those terms.
Task 8: fix round 1/5 (Critical + 3 Important + 2 minors; resume implementer)
Task 8: fix round 1/5 implementer DONE at 3fd6951 (-race and verify green; 10 new tests, 7 failing pre-fix with the reviewer's own symptoms, 3 guards, per-test table in the report). ps-error path now sends no window, notices and does not latch; a per-model gate held across the ask makes 8 concurrent callers produce one prompt and one answer; context_tokens decoded through a *int shadow with the literal dropped from Default(), omitempty so a derived budget is not persisted as a hard 0, a warning naming the unused tokens, and agent.New taking its initial reserve from History.Budget; approver behind a locked SetApprover. Flagged outside the task: doctor still compares the window against cfg.ContextTokens and never mentions context_window.
Task 8: fix round 1/5 re-review (opus, own -overlay probes) — all five findings FIXED (ps-error sends num_ctx 0 and stays unlatched so the question survives; 8 concurrent Applies give 1 ask and one shared answer for yes and for no, -race clean; budget absent/16384/explicit-0/65536 all behave, and the user's real config with context_tokens 32768 is unchanged; agent.New's reserve is sane at budget 0, -5, 1, 512 and 200000). Test-claim correction: TestDerivedBudgetUsesTheWholeWindowQuietly is called a guard but fails pre-fix, and the two SetProvider latch tests are claimed as failures but pass at 2f832e0 — they only fail against 23e50bf. RESIDUAL CONSENT HOLE found at loader.go:195-207, in this task's own round-0 code: with no configured window and a resident model whose /api/ps omits context_length, ContextLength falls back to the Modelfile and setWindow(4096) goes on the wire for a model loaded at something else, nobody asked. New minors from the fix diff: SetModel still reads Cfg.ContextTokens (now 0) so the reserve drops 4096 to 1024; the clamp warning collapses to "clamped to 4096 … OLLAMA_CONTEXT_LENGTH=4096"; config.go is gofmt-dirty; a reentrant Apply from inside the approver deadlocks, which is a Task 9 hazard if /model calls it from the Bubble Tea update goroutine.
Ruling T8-b: the residual consent hole is fixed here, not parked to Task 9. Consent is the rule that outranks everything on this branch, the hole is in this task's own code, and the correct shape already exists eleven lines above it. Costs one more short round.
Task 8: fix round 2/5 (consent hole + 4 minors; resume implementer)
Task 8: fix round 2/5 implementer DONE at 6e66027 (gofmt, vet, -race and verify all clean; 6 tests, 5 failing pre-fix — one of them by HANGING — and 1 guard, with the baseline named per row). T8-b: the no-configured-window arm now reads Resident first and branches the same three ways as the configured arm, falling through to ContextLength/the Modelfile only when nothing is loaded, where adopting it evicts nobody; the two shared outcomes are named functions (unknownResidency, residentUnreportedWindow) so the arms cannot drift apart again, which is how this was missed, and a duplicate /api/ps on the resident path is gone. Reentrancy solved both ways: the mutex became a per-model in-flight channel selected against the caller's ctx, so any wait degrades to keeping the server's window and latching nothing, with the one unrescuable shape documented at the site. Report correction acknowledged rather than silently edited.
Task 8: fix round 2/5 re-review (opus, full enumeration) — all five findings FIXED. Consent enumeration clean in BOTH arms across all five server states: every wire write is either the window the server already holds, a window for a model nothing holds, or a consented one; /api/show is never reached after a failed /api/ps and /api/generate is never called. The named-function refactor genuinely shares both outcomes, and dropping ContextLength's duplicate /api/ps fixes the disagreement case that caused the bug. All five claimed pre-fix failures reproduced verbatim, including the cancellable-wait test HANGING to a timeout.
Task 8: complete (commits 23e50bf..6e66027, review clean after 2 fix rounds)
CARRY INTO TASK 9: (a) ruling T8-a — re-run Apply on a goroutine once Registry.Approve is wired in runInteractive/runSessionHost, and route loader notices through Agent.Events; without both, the consent prompt exists nowhere a user can see it and a default-config user silently runs at whatever the server held; (b) Minor loader.go:297 — the ctx.Done arm calls keep(), which writes serverWindow, so a cancellation landing as the claimer releases puts the server's window on the wire AFTER the consented one (reproduced 221/400 with coincident timing; always downward, so never an eviction or a consent breach); fix by returning serverWindow without calling keep(); (c) OnEvicted/OnWindowChanged are dead code until checkBackend routes through the loader; (d) cmd/commands.go doctor still compares the window against cfg.ContextTokens and never mentions context_window.
Task 9: dispatched (BASE 6e66027, implementer opus)
Task 9: implementer DONE_WITH_CONCERNS at ffd8c10 (-race across agent/loader at -count=5, tui/cmd/ui at -count=3, concurrency tests at -count=10; verify and gofmt clean). Beyond the brief and deliberately: SetProvider rebuilds the loader via a new agent.LoaderFactory, because without it /provider then SetModel resolved against the OLD backend's loader — sending one machine's num_ctx to another and asking consent about a server the user had just left, which is a consent breach in its own right. Also fixed a pre-existing race the new goroutine exposed: applyReserve read History.Limit() outside the lock it had just written Reserve under. Plain-mode divergence from T8-a's literal wording: plain uses ResolveModelNow inline from a new REPL.OnStart hook rather than a goroutine, because promptCtx reads the same r.lines channel the REPL loop does and a second goroutine would race the user's keystrokes for the answer; the TUI (in-process and hosted) uses the goroutine and the shared modal, with Session.NewView replaying an open ask so the hosted case works when the question is raised before any terminal attaches. Concerns: (1) the post-switch compaction is exclusive with the tool loop and a concurrent SetModel but NOT with /compact and /clear, which mutate History.Messages from a UI goroutine; (2) Agent.Model/Profile stay plain exported fields read from 69 UI sites, and the post-switch Compact is a new off-loop reader; (3) refreshKeepAlive still reads cfg.KeepAlive rather than the loader's per-model params and reaches Warm → /api/generate, the last path outside the loader that can load a model; (4) nothing here is exercised against a real backend.
Task 9: reviewer dispatched (opus)
Task 9: review (opus, overlay probes) — Spec ✅ / Quality changes requested. RULING T8-a VERIFIED as user-visible behaviour in all three modes: wireEvents sets Approve and Events inside NewSession and ResolveModel is called strictly after it, the hosted case replays an ask raised before any terminal attached (proven by a TUI test and by a cmd test driving the REAL loader against an httptest Ollama), and notices reach the transcript with stderr only as a fallback while no UI exists. Both deviations accepted: LoaderFactory's claim verified (both UIs do SetProvider then SetModel, so the old backend's loader would have resolved the new one), and plain mode's inline resolution is right because promptCtx reads the same channel the REPL loop does. Findings, four Important: (1) ApplyWindow writes Budget/Reserve from the resolution goroutine before taking turnMu, so History.Limit/Target/Over and five UI readers race a running request — reproduced under -race; (2) /clear and /compact mutate History.Messages without turnMu, racing the same goroutine — reproduced; (3) Compact reads a.Model/a.Profile off-goroutine; (4) Warm posts /api/generate with no options, so a post-request keep-alive on an evicted model loads it at Ollama's default window, outside the loader — a genuine 10.1 exception. Minors: Ctrl-D at the plain consent prompt wedges Run forever; the plain prompt uses context.Background so the resolve timeout never withdraws it; sessionLoader is now written from a UI goroutine.
Task 9: fix round 1/5 (4 Important + 3 minors; resume implementer)
Task 9: fix round 1/5 implementer DONE at be64e7c (-race clean, touched packages at -count=3, verify and gofmt clean, every new test verified failing pre-fix against ffd8c10). Important 2 needed a change the review did not ask for: putting /clear behind Agent.ClearHistory exposed a lock-order inversion — in a served session the order is turn lock → session lock, because everything holding the turn lock writes to the Session as it goes and that takes the lock View.Update holds for its whole body, so /clear waiting for the turn lock inside Update would hang the terminal; it moved off the event loop beside /compact, with a test that fails after 2 s against the synchronous version, and ClearHistory's doc now states the rule instead of the earlier wrong reasoning that "no request in flight" made it safe. Two documented minor deviations: no separate EOF check after OnStart (the loop's first nextLine already reports it), and ResolveModelNow no longer applies the timeout itself because cmd's OnStart applies it to a context both the resolution and the REPL prompt wait on — a deadline inside the agent was invisible to the prompt the loader raises through the UI, so it could never have withdrawn it.
PARKED, pre-existing, for the final whole-branch review: Agent.Model/Profile are plain exported fields at ~69 read sites, and History.System.Content is written by SetModel under modelMu while run()'s maybeCompact reads it through Tokens() unguarded — unchanged by this branch (SetModel has always written it from a Bubble Tea Update) but reachable via the model picker mid-run. Closing it needs the 69-site accessor refactor or a guard on History.System.
Task 9: fix round 1 scoped re-review dispatched (opus)
Note 2026-09-19: the Task 9 fix-round-1 re-review (opus) died on an API weekly rate limit before reading anything; no verdict was produced. Session is back on Fable 5.1; commit trailer returns to "Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>". Opus seats may still be rate-limited until 15:00 Pacific on 2026-09-19 — use fable or sonnet for dispatches until then.
Task 9: fix round 1 scoped re-review RE-dispatched (fable)
Task 9: fix round 1/5 re-review (fable, own probes and mutation reverts) — all five findings FIXED; the lock-order change judged sound (only run/ClearHistory/CompactNow block on turnMu and every TUI caller is off Update; resolveModel uses TryLock; the hang test genuinely fails against a synchronous /clear; a three-view stress probe showed no deadlock); all nine claimed pre-fix failures verified by reverting each hunk. NEW: N1 Important, a REGRESSION of this round — SetModel's goroutine lost its 2-minute deadline when the code moved out of resolveModel, so a hung consent or post-switch Compact holds turnMu forever and wedges every later request; N2 Important — Agent.Resume rewrites the transcript without turnMu, and the probe shows a parked post-switch compaction replacing the RESUMED transcript with the old conversation's summary, which the next autosave writes into the resumed session's file; N3 Minor, introduced this round — SetSession now runs without s.mu and -race flags it against headerView; N4 Important, pre-existing — when the model is not loaded, Status returns the Modelfile's num_ctx and checkBackend's window-changed arm overrides OnEvicted, so a configured 32768 became 4096 on both chat and Warm; N5 Important, pre-existing — provider options are not per-model, so while /model other awaits consent num_ctx stays at the previous model's 32768 although other is resident at 8192, and a request in that window reloads it WITHOUT CONSENT; N6 Minor — Budget and Reserve are written in two critical sections, so a reader can briefly see Limit() at its 512 floor.
Ruling T9-a: N4 and N5 are fixed in this task, not moved to Task 10. Same reasoning as T8-b: both break the rule that only the loader decides num_ctx, N5 is an unconsented reload of another application's model, and Task 10 is documentation and an end-to-end script, the wrong home for a consent fix. Costs one more round on Task 9.
Task 9: fix round 2/5 (N1-N6; resume implementer)
Task 9: fix round 2/5 implementer DONE at d7f3e2a (-race clean, touched packages at -count=3, verify and gofmt clean; six new tests each verified failing pre-fix by reverting its own hunk). N1 closed by collapsing both spawn sites into one Agent.goResolve, since two copies of the wrapper is how the deadline went missing; N3 needed more than asked: Resume calls SetSession from inside itself where no caller can wrap it, so the pointer got its own guard (Agent.sessionMu plus a CurrentSession() accessor used by headerView and ownCode), which also caught a genuine cross-goroutine read in picker_test.go. Standing pre-existing item unchanged: Agent.Model/Profile bare at ~69 sites and History.System.Content vs maybeCompact. Scoped re-review dispatched (fable, same reviewer)
Task 9: fix round 2/5 re-review (fable, own probes and mutation reverts) — N1-N6 all FIXED; lock order verified as turnMu → modelMu → History.mu with sessionMu a leaf and turnMu released before s.mu; the same-window switch re-applies num_ctx; typing during the /resume gap is consistent. NEW: R1 Important, N5 incompletely fixed — resolveModel's l.Apply writes provider options BEFORE the stale-generation check, so a slow "/model big" followed by a fast "/model small" leaves small with num_ctx=65536 on the wire while small is resident at 8192 for another client, and the next request reloads it without consent; R2 Important, present since ffd8c10 — in plain mode /model raises the consent prompt from the resolution goroutine while the main loop is already reading r.lines, so the user's "y" reached the main loop AS A NEW REQUEST TO THE MODEL and the prompt then swallowed a later line; R3 Minor — the shared modal waits on rootCtx, so the resolve deadline cannot withdraw it (it does not hold the turn lock while parked); R4 Minor, introduced this round — Resume writes a.handoff from a goroutine while the busy-safe /handoff reads it, flagged by -race.
Ruling T9-b: all four fixed in round 3. R1 is an unconsented reload; R2 turns a consent answer into a prompt to the model, which is both a consent failure and a wrong action taken on the user's behalf; neither is deferrable. Costs round 3 of 5.
Task 9: fix round 3/5 (R1-R4; resume implementer)
Task 9: fix round 3/5 implementer DONE at dde2fde (-race clean, touched packages at -count=3, verify and gofmt clean; four new tests each verified failing pre-fix by reverting its own hunk). R1: reapplyCurrent, a bounded loop (8 rounds and the context) that clears and re-Applies the current model and re-checks the generation after each Apply, touching only the wire — no budget, no compaction — so every exit leaves the current model's window or none. R3 needed a seam: tools.ApproveCtxFunc + Registry.ApproveCtx, preferred by the loader via SetApproverCtx and wrapping the plain approver when a UI offers none; the TUI hands the asker's context to Ask, which already withdraws through CancelAsk to every view; plain mode wired the same way so both UIs answer deadlines by one mechanism. Scoped re-review dispatched (fable, same reviewer)
Task 9: fix round 3/5 re-review (fable, own probes) — R1-R4 all FIXED: eight orderings of three and four overlapping switches each ended with the current model's window on the wire and a matching budget, and a late yes for an overtaken model did not overwrite the current one; in plain mode y, n, Ctrl-C and EOF all reach the prompt and nothing reaches the model; the modal closes on both terminals at the deadline with no parked goroutine; no handoff race. NEW: S1 Important, introduced by round 3's R3 fix and BLOCKING — loader.go:372-376 latches asked/agreed=false whatever the reason approve returned false, so a question withdrawn by the deadline is recorded as the user's refusal and a user away for two minutes is never asked again for the rest of a hosted session; S1b Minor — a consent question raised while another approval is open is refused unseen by Ask and latched as no; S2 Minor, fails safe — loop.go:527 returns on err or w<=0 before the stale check, so an overtaken resolution whose /api/ps fails clears the current model's window to none and leaves the old model's passthrough options on the new model's requests; pre-existing park: tui/session.go:773 reads Tokens() outside the turn lock.
VM-VERIFICATION ITEM for the user: the whole consent design assumes that sending NO num_ctx leaves a loaded model alone on a real Ollama. No reviewer could check that offline; confirm it on the LAN box before relying on the gate.
Ruling T9-c: S1 and S2 fixed in round 4 (S2 is a line move and is cheaper to fix than to carry); S1b parked for the whole-branch review — it needs a refusal distinguishable from an answer, which is a seam change, and it fails toward not reloading. Costs a fresh implementer seat for a small diff.
Task 9: fix round 4/5 (S1 + S2; FRESH implementer on fable, per the rounds-4-5 escalation)
Task 9: fix round 4/5 implementer (fresh, fable) DONE_WITH_CONCERNS at efecd42 (-race clean incl. loader and agent at -count=3; verify and gofmt clean). S1: a withdrawn question returns through keep() before anything latches and raises no notice, since the "clamped" line shares the model's notice key and would swallow a later real refusal's explanation; pre-fix the test reports "asked 1 times; a withdrawn question was recorded as the user's refusal". S2: the stale check moved directly after Apply; pre-fix "num_ctx 0 on the wire for model small, want 8192" and "top_k 7 ... want 40", proven with the real loader and the real provider against httptest. Noticed, not touched: reapplyCurrent on an already-dead context (the normal way to be superseded and late) calls Apply(deadCtx), the /api/ps read fails on the context, and unknownResidency blanks the current model's resolved window with a one-time UNTRUE notice that the backend could not be read; fails safe, predates this round, slightly easier to reach now that a withdrawn question returns on exactly such a context; likely fix is re-applying on a fresh short context. One red run reported honestly: internal/tui's pinColorProfile flake family (TestServedSessionRendersEachClientAtItsOwnSize then two neighbours with ANSI in plain transcripts), not recurring in five further runs, in packages this round does not touch.
Task 9: fix round 4 scoped re-review dispatched (fable, same reviewer)
Task 9: fix round 4/5 re-review (fable, own probes) — S1 and S2 FIXED: with the real loader and two views the modal closed on both terminals at the deadline, the session kept the server's 8192, the next ResolveModel raised the modal again, and a genuine no then latched with exactly one notice; the overtaken-and-failing probe ended on small with num_ctx 8192 and big's top_k gone; both fix-proving tests fail against dde2fde. Verdict: Task 9 can close.
Task 9: parked for the whole-branch review, each with its fix named: (P1) Minor — reapplyCurrent on a dead context blanks the current model's resolved window and shows an untrue "could not read the backend's loaded models" notice; reachable by a double switch plus a modal left unanswered for two minutes; fails safe; fix at loop.go:396, re-apply on a fresh 10-second context.Background() timeout. (P2) Minor — after a withdrawal the user is never told nobody answered; tui/ask.go:141 calls CancelAsk with an empty note; pass "no answer; question withdrawn, asked again on the next switch". (P3) Minor, S1b — a consent question raised while another approval is open is refused unseen and latched as no; needs a refusal distinguishable from an answer. (P4) pre-existing — tui/session.go:773 reads Tokens() outside the turn lock. (P5) pre-existing — Agent.Model/Profile bare at ~69 sites and History.System.Content vs maybeCompact. (P6) oddity — an overtaken model's consent modal can open after the session has already moved on.
Task 9: complete (commits 6e66027..efecd42, 4 fix rounds, 6 parked)
Task 10: dispatched (BASE efecd42, implementer opus)
Task 10: implementer DONE_WITH_CONCERNS at b6b4be4 (verify and -race green; e2e 10/10 consecutive with [PASS] task, [PASS] native, E2E PASS). Non-vacuousness as run: E2E_TASK_ENGINE=false printed TREE:no COMPACT:no, never [PASS] task, exit 1, its last notice being the OLD behaviour "compaction failed (empty summary: …); falling back to trimming"; the sentinel lives only in a workspace file read by shell cat and TREE:yes is asserted only on the system prompt after "Working memory:"; native with context_window 0 printed NUMCTX:0 and exited 1; consent arranged away honestly by the fake /api/ps reporting nothing resident. FOUND A LIVE REGRESSION on the branch: the pre-existing engine e2e scenario was already red at efecd42 — the recorder merged a read's durable file record from the item_cap-capped raw item, so a 300-line read recorded lines 1-221 and the already-read footer could never fire again for any file over 4 KiB; unit tests missed it because they all use small files; fixed in record.go with two tests. Root CLAUDE.md (outside be-code's repo) edited in place, not in the commit. Documented the code where brief and code differed, notably that "a" at the reload prompt saves reload_on_mismatch: always for the SERVER, not per model. Concerns: the task scenario forces compaction with context_tokens 1200, so a change to Limit()'s floor or maybeCompact's six-message guard would move which call compacts (fails loudly as COMPACT:no); the e2e proves the wire, not the consent gate; README's Status section still says v0.3.0.
Task 10: reviewer dispatched (sonnet)
Task 10: review (sonnet, own runs and breaks) — Spec ✅ / Quality approved, no Critical or Important. Both scenarios verified non-vacuous by the reviewer itself: engine disabled → rc=1 TREE:no COMPACT:no with the old "falling back to trimming" notice; context_window 0 → rc=1 NUMCTX:0; the assertion reads the 4th call's system prompt, i.e. post-fromTaskRecord, after "Working memory:"; the mock genuinely returns an empty summary and the harness took the task-record path. record.go fix verified by swapping the pre-fix file in: both new tests fail without it, and the verbatim buffer is still capped. Every documentation spot-check accurate against the code. Minor: README.md:782 "Status" still says v0.3.0, now contradicting the 0.11.0 changelog.
Task 10: complete (commit b6b4be4, review approved, no fix round)
ALL TASKS COMPLETE (1-10). Branch feature/context-handling at b6b4be4, merge base b74c36d.

## Consolidated parked list for the final whole-branch review
MUST-FIX (ruling T3-g): store.go Flush rewrites a split source document before writing the split-off root's new file, so a failure on the second write erases a hand-added task; fix: write roots whose name is new before rewriting the document they were split out of.
FIX-READY one-liners: (T5-d) spentUnfiled should also require StatusDropped and a reason starting "adopted by "; (T9 P1) reapplyCurrent on a dead context blanks the current window with an untrue notice — re-apply on a fresh 10 s context; (T9 P2) CancelAsk with an empty note never tells the user nobody answered; README.md:782 stale Status line.
DESIGN-LEVEL: (T5-b follow-up) no engine call is time-bounded, a wedged store takes the session at the next composeSystem; (T9 P3/S1b) a consent question raised while another approval is open is refused unseen and latched as no — needs a refusal distinguishable from an answer; (T9 P5) Agent.Model/Profile bare at ~69 sites and History.System.Content written by SetModel while maybeCompact reads it; (T9 P4) tui/session.go:773 Tokens() outside the turn lock; (T9 P6) an overtaken model's consent modal can open after the session moved on.
ACCEPTED RESIDUALS: (T3-e) a kill between the migration's rename and its flush leaves the old store as *.migrated-<stamp> with nothing lifted, recoverable by renaming back; (T3-f) unsavedStateError can lose a turn counter and baseline; restoreLegacy discards rename errors; Task 1 minors (Add accepts empty text; live *Node pointers).
PARKED PROMPT QUESTION (T5-a): taskGuidance left verbatim — decide on the VM.
USER VM CHECKS: native thinking levels against a thinking and a non-thinking model; that sending no num_ctx leaves a loaded model alone; a mismatching context_window in a hosted TUI raising the modal on the attached terminal.
FLAKES, pre-existing: internal/live TestKeyPumpEscapeRaceStress and TestResizeSendsNoSizeFrameToOtherClients; internal/tui pinColorProfile family.

FINAL WHOLE-BRANCH REVIEW (fable, probes) — verdict "not yet; safe once C1, C2 and I1-I4 are fixed". CRITICAL: C1 — rawBlock renders the doing node's raw buffer with no cap, and a model that never calls task (the common case) leaves everything on a permanent unfiled doing node, so Render(6144) returns ~36 KB (~12k tokens); at a 16k window that forces 7 summary calls where engine-off needs 0, the compaction request embeds the block and overflows the window (producing the very empty summaries this branch exists to survive), and after a switch to a smaller window the system prompt cannot be shrunk so every request is silently truncated. C2 — Flush never checks s.docs, so a document the user edits mid-session is overwritten (the README tells them to edit freely), and with two live stores on one workspace B's decision note is erased by A's next flush. IMPORTANT: I1 a fresh session inherits the previous session's open task; I2 reviewer and co-worker providers are bare NewOllama instances that skip the loader, and the reviewer believes Ollama's scheduler treats "no num_ctx" as "the default" rather than "whatever is loaded", which would make every no-window send a reload; I3 the options passthrough is merged last so a configured num_ctx (or num_batch/num_gpu/use_mmap) bypasses consent; I4 hosted-mode warnings go to the host log, including the context_tokens cap line; I5 Ask refuses a second non-picker prompt, so a consent modal makes the agent's next write approval count as denied and vice versa; I6 only observe and resolveModel recover from panics; I7 a symlinked .be-code or tasks redirects unapproved writes outside the workspace. Minors M1-M4. Triage: T3-g's recorded fix is WRONG as written (writing new-name documents first breaks migrate.go:148's root-order assumption and brings back the duplicate migration; flushError must carry the set of names written).
Ruling F-1 (spec amendment, surfaced to the owner): spec §6.1 says the active branch and its verbatim step are never cut, while §4.3 allows a 32 KiB node and §6 caps the block at 6144 bytes — the spec contradicts itself, and following §6.1 literally is what produced C1. The budget wins: the whole block is capped at a byte budget derived from the live History.Limit() (about 25%, floor 2 KB), raw items render newest first within it and older ones as distilled one-liners; the full verbatim buffer stays in state and reachable with task show. Costs the literal "lossless" promise for anything older than the newest items, which was never deliverable at a 16k window.
Ruling F-2: wherever the loader or checkBackend KNOWS the resident window, it sends that window rather than none; "no num_ctx" is kept only for the case where residency genuinely cannot be read. This removes the branch's dependence on the unverified assumption that no num_ctx leaves a loaded model alone. Costs nothing if the assumption was true.
Ruling F-3: the VM check of what Ollama does with an absent num_ctx is now a MERGE GATE for the owner, not a nice-to-have.
FIX WAVE: dispatched (BASE b6b4be4, implementer fable, brief final-fix1.md)
OWNER EVIDENCE 2026-09-19: on the owner's real Ollama, a request with no num_ctx runs at the server default (8192 on their box) — it is what the pre-0.11 harness got before they set the context by hand. So an absent num_ctx means "reload to the server default", NOT "leave the loaded model alone"; the final reviewer's suspicion is confirmed. This upgrades Ruling F-2 from a precaution to a requirement, and the one remaining no-window case (residency unreadable) is merely least-bad, not safe. Relayed to the fix-wave implementer mid-run: send the known resident window everywhere it is known, make the no-window notice truthful, and replace the docs' "unverified" label with the known behaviour. The merge-gate VM check (F-3) narrows to confirming the fixed build, not discovering the behaviour.
FIX WAVE: the fable implementer died on a session rate limit with C1, C2, I1, I6, T3-g, T5-d, M1, M2 done but uncommitted. Controller took over at the owner's direction: verified the tree (verify, -race on engine/agent/cmd, e2e all green, real config untouched) and committed it as part 1. Remaining, done by the controller directly: I2, I3, I4, I5, I7, P1, P2, M3, M4, README status, docs on absent num_ctx.
FIX WAVE: completed by the controller at the owner's direction, in three commits b6b4be4..HEAD: part 1 f6084f9 (the dead implementer's verified work: C1, C2, I1, I6, I7, T3-g corrected, T5-d, M1, M2), part 2 002f89b (I3, I4, P1, P2), part 3 (I5, I2 with F-2, M3, M4, README status, docs on absent num_ctx). Verified by the controller: make verify PASS, go test -race ./... clean, e2e 10/10 consecutive with [PASS] task and [PASS] native, gofmt clean, real ~/.be-code/config.json unchanged. Note: controller fixes skip the usual implementer/reviewer separation, so the one scoped re-review below covers all three parts, with most weight on part 1, which the controller verified by test but did not read line by line.
FIX WAVE: scoped re-review dispatched (opus)
FIX WAVE scoped re-review (opus, own probes) — MERGE VERDICT: safe as it stands. C1 measured: uncapped 37,477 B → 4,542 B @8k, 5,142 B @16k and @32k, 1,782 B at the 2k floor; summary calls 0 vs 0 engine-off at 16k/32k; newest raw item always present; a dropped go build kept its CmdRef and error; task show still returns the full 35,723 B buffer. C2 held under attack (deleted node with new evidence returns with it; user's status wins; reorder kept; unparseable mid-session → .edited aside; true write-after-hash race → aside; no path deletes a document). T3-g corrected holds, both migration windows behave. I1, I6 (fault injected on all 11 fenced ops), I7, T5-d, M1, M2, part 2 and part 3 all PASS (four queued asks each answered; quit releases all; secondaryLoad never asks and puts 8192 on the wire).
FOLLOW-UPS (none blocking): (1) Important, residual by design — between /model and its resolution clearWireWindow sends nothing, which on the owner's server is the default and a possible unconsented reload; no correct number exists yet for the new model, so the fix is to hold the first request until the resolution lands or read /api/ps synchronously in the switch; (2) Minor — durable notes lose oldest lines silently, where spec 6.1 says each section says how many are not shown; (3) Minor — migrate.go picks its own document name before Flush's reconcile, so a document appearing during OpenAt could reopen the duplicate; have Flush return the names it chose; (4) Minor — /task, /notes and the exit flush call the engine outside the fence; (5) Minor — a queued ask released by quit reads as a denial. Taken now: the task schema's missing decision field.
CONTROLLER VERIFICATION at HEAD: make verify PASS; e2e PASS; working tree clean; real config unchanged.
