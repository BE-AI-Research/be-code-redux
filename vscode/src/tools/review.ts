import * as vscode from "vscode";
import { ToolRegistry } from "./registry";

// Stub for Task 11: editor-side review of file changes (diff view).
export function registerReviewTool(_reg: ToolRegistry, _ctx: vscode.ExtensionContext): void {}
